# 暗号化ファイルについて

拡張子 `.enc` のファイルは AES-256-CBC（OpenSSL, PBKDF2）で暗号化されています。
公開ページの動作には不要なファイル（設計メモ・SQL・サーバー側コード・様式など）です。
復号の鍵は総務（佐川）が管理しています。必要な場合は担当者へ依頼してください。

復号コマンド（鍵ファイルを持つ人だけ）:
```
openssl enc -d -aes-256-cbc -pbkdf2 -iter 200000 -in <file>.enc -out <file> -pass file:<鍵ファイル>
```
