# 薬品在庫管理 v0.8 作業ログ

## 2026-09-02 初回実装・本番公開

### 概要
- 企画書・仕様書（`output/薬品在庫v08_企画書_20260902.html` / `薬品在庫v08_仕様書_20260902.html`）に基づく総合UI一新版。
- コンセプト「夜は3タップで出せる。昼はすべてが繋がる。」
- v0.7 と同じDB（Supabase pharmacy_*）で並行稼働。**v0_7/ 配下は1文字も変更していない**（git status で確認済み）。
- Phase 0（DBサポート層 `scripts/pharmacy_v07/10_v08_support.sql`）は適用済みの状態から着手。

### 構成
```
v0_8/
├── index.html   … 全画面のDOM骨格（約430行）
├── app.css      … デザイントークン＋全スタイル（day/night両テーマ）
└── js/          … db / store / theme / ui / screen-*（8画面）
```
- テーマ: 20:00–5:59 = 夜間ダーク自動。手動 day→night→auto 巡回（localStorage `p8_theme`）。60秒再判定・200msクロスフェード。
- レイアウト: 768px未満=ボトム4タブ（出庫/在庫/履歴/管理）、以上=左サイドバー8項目＋件数バッジ。
- 動きは仕様書§06の9個のみ。`prefers-reduced-motion` 一括対応。
- GAS: recordPrescription（出庫時・fire-and-forget）とカルテ突合（読み取り）のみ維持。batch/addMedicine/getStock は廃止（ユーザー決定）。
- 入庫は入荷画面へ一本化しつつ「簡易入庫モード」（ロット情報なし）を残置（ユーザー決定）。
- 担当者10名はハードコード継続（ユーザー決定）。

### 実施した動作テストと結果（すべて本番DB・テスト行は即時削除）

| テスト | 結果 | 証跡 |
|---|---|---|
| 出庫: QR同一薬集約・長押し0.6秒確定・0.3秒では送信されない | ✓ | batch[M055:2]→tx1行のみ・operator/occurred_on/source入り |
| 出庫: 外用薬(stock_untracked)は記録のみで在庫不変 | ✓ | M055 93→93 |
| 出庫: オフラインキュー（fetch遮断→琥珀バー→復帰後自動再送・二重送信なし） | ✓ | queue1→再送1回→2回目flushで増加なし |
| 出庫: 発注点割れ警告行（残数・消費ペース日数） | ✓ | M005で警告文言確認 |
| 棚卸: 2品目入力→±0はstocktakesのみ・差分ありはadjust+在庫書換（RPC単一Tx） | ✓ | stocktakes2行/adjust1行/diff_qty生成列 |
| 棚卸: 下書きlocalStorage・反映後クリア | ✓ | draft 2品目→反映後null |
| ウィザード: 厚労省マスタ選択で名称/カナ/薬価/単位/レセ電が自動充填 | ✓ | アモキシシリン細粒10% |
| ウィザード: 外用薬(form=外用薬)はトグル初期OFF＋分類「外用/」強制 | ✓ | アクアチム軟膏で確認 |
| ウィザード: M203採番・初期在庫30のin記録・v_medicines(=v0.7読み先)へ即出現 | ✓ | official_name結合まで確認 |
| マスタ整備: 原価セル編集→保存→未設定数減（43→44）→復元 | ✓ | v_master_gap_stats実測 |
| マスタ整備: 対象外トグルON/OFFで充足数増減 | ✓ | M012で確認・復元済み |
| 発注: カート→下書き発注書作成→「発注した」でsent | ✓ | PO-20260902-1-s3p |
| 受入: 半量250受入→partial・残250が次回初期値→全量でreceived | ✓ | received_qty 250→500 |
| 受入: 期限4桁「2807」→2028-07-31でロット登録・v_expiry計算対象化 | ✓ | days_to_expiry=698 |
| 個別入荷（通常）: ロット+期限2712→2027-12-31+in+在庫加算 | ✓ | lot/tx/stock三段確認 |
| 個別入荷（簡易入庫）: ロットなし・in記録+在庫加算のみ | ✓ | lot_id=null |
| 履歴/消費量: v0.7と同一クエリ・月次入荷金額の小計行追加 | ✓ | tx総数1811/表示上限1000はv0.7と同挙動 |
| 今日の状態: KPI5個・要対応リスト実名表示・行き先リンクでフィルタ引継ぎ | ✓ | 在庫切れ→発注(alert) |
| テーマ: day→night→auto巡回・localStorage保存・autoで時刻判定 | ✓ | 15時台=day復帰 |
| サイドバーバッジ | ✓ | 発注18/マスタ整備60/要対応19 |

### テストデータ削除の証跡（すべて実行結果確認済み）
- 出庫テスト tx（patient_name='V08動作テスト2/3'）: REST DELETE 各1行 → transactions=1811 復帰
- 初回出庫テスト3行（id1886-1888）はコーディネータ側で削除・在庫復元済み（M001 390 / M002 335 / M055 93）
- 棚卸: adjust tx id1891 REST DELETE、stocktakes 2行を直SQL DELETE → `remaining_stocktakes=0`
- ウィザード: in tx id1892 REST DELETE、M203を直SQL DELETE → `medicines_total=103` / `next_code='M203'`
- 発注受入: tx id1893-1894 REST DELETE、lots(V08TEST/V08TEST2)+order_items+orders を直SQL DELETE
- 個別入荷: tx id1895-1896 REST DELETE、lot(V08TEST3) 直SQL DELETE
- **最終突合**: orders=0 / order_items=0 / lots=182 / stocktakes=0 / transactions=1811 / medicines=103（テスト前と完全一致）
- GAS側（処方履歴等）へのテスト送信はテスト中 `P8.db.gasGet` をスタブ化して遮断

### 既知の注意・残課題
- **iPhone Safari実機でのQRカメラテストは未実施**（開発PCはカメラ権限なし）。QR読取ロジックはv0.7と同一の html5-qrcode 2.3.8 移植。実機での §5.4-1〜3 の最終確認は現場初回利用時に要確認。
- 履歴の読み込み上限はSupabase側の1000行キャップ（v0.7と同挙動・仕様書の「v0.7と一致」を満たす）。
- 期限未登録ロット一覧: 既存182ロットは全て qty_remaining=0 のため表示対象外（仕様書§6.2のクエリどおり。今後の入荷分から期限が育つ）。
- 体重別用量表はコード直書き移植（DB化は将来課題・要確認事項⑤どおり）。
- portal.html は未変更（ユーザー承認後に切替。それまで v0_8/ 直URL運用）。
- テスト中に一度だけバッチへ意図しないM001追加が発生（06:38）。90秒監視・コード精査・正規化衝突全数チェック・カメラ状態確認では再現も原因も特定できず（可視タブでの手動操作の可能性が高い）。以後の全テストで送信内容=バッチ内容の一致を毎回検証し、混入は再発していない。

### 検証4通りスクリーンショット
`C:\ClaudeWork\ss\v08_*.png`（day/night × mobile/desktop × 全8画面 = 32枚。本番URLで撮影・console エラー0）

### 公開後の追加修正（2026-09-02 同日）
- `baaf098` 表示ユーティリティ `.hide-mobile/.hide-desktop` が `display:block` で KPIバンド(flex)・カードグリッド(grid) を上書きし崩れていた問題を修正（隠す側だけ `display:none !important` に変更）。デスクトップのヘッダーにブランドが二重表示される点も解消。
- `f0a4a44` CSS/JSリンクに `?v=0.8.1` を付与。GitHub Pages の10分キャッシュで旧アセットが残る問題の恒久対策。**今後 app.css / js を変更したら、この版数も上げること。**
- v0.7回帰再確認（テスト完了後）: v0.7画面で103品目・在庫非管理15・M001=390/M002=335/M055=93/M009=0 を実画面で確認＝無傷。
